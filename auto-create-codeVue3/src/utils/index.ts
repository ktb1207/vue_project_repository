export { httpAxios } from './configAxios';

const utils = {
  showSuccessMessage: (msg: string): void => {
    alert(msg);
  },
  showWaringMessage: (msg: string): void => {
    alert(msg);
  },
  showErrorMessage: (msg: string): void => {
    alert(msg);
  }
};

export { utils };
